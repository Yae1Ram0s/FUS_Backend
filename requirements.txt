# Dependencias directas del backend (Django + Channels/ASGI).
# Generado a partir del entorno virtual instalado (venv) — versiones
# exactas fijadas para reproducibilidad.

Django==6.0.6
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0

# ASGI / WebSockets (notificaciones en tiempo real)
channels==4.3.2
daphne==4.2.2
# Requerido solo cuando CHANNEL_LAYERS usa Redis (ver backend/settings.py:
# se activa automáticamente si la variable de entorno REDIS_URL está
# definida; sin ella se usa channels.layers.InMemoryChannelLayer y esta
# dependencia no hace falta en tiempo de ejecución, pero se deja fijada
# aquí para que un despliegue con Redis no tenga que recordarlo aparte).
channels-redis==4.3.0

# Backend de CACHES cuando REDIS_URL está definido (ver backend/settings.py);
# sin ella se usa LocMemCache y esta dependencia no hace falta en tiempo de
# ejecución, mismo criterio que channels-redis arriba.
django-redis==7.0.0

# Cola de tareas real para notificaciones (correo + PDF adjunto), sobre el
# mismo Redis de arriba — reemplaza los threading.Thread sueltos cuando
# REDIS_URL está definido (ver services/notificaciones.py; sin Redis se usa
# el hilo suelto de respaldo, sin cambios). rq es dependencia directa de
# django-rq, pero se fija aparte porque notificaciones.py importa rq.Retry.
django-rq==4.1.1
rq==2.10.0

# Base de datos (MySQL) — driver 100% Python: no requiere compilador ni
# librerías de MySQL instaladas en el sistema (a diferencia de mysqlclient),
# así que se instala igual en Windows (dev) y Linux (servidor institucional)
# sin condición de plataforma. Se activa como reemplazo de MySQLdb en
# backend/backend/__init__.py.
PyMySQL==1.2.0

# Base de datos (PostgreSQL/Neon en producción, ver backend/settings.py:
# DATABASE_URL) — [binary] trae los binarios precompilados de libpq, sin
# necesidad de compilador ni headers de PostgreSQL instalados en el sistema
# (equivalente al criterio de PyMySQL arriba, pero para Postgres). Sin esto,
# Django truena con "ImproperlyConfigured: Error loading psycopg2 or psycopg
# module" en cualquier despliegue que use DATABASE_URL con esquema postgres.
psycopg[binary]>=3.2,<4

# Sirve para leer DATABASE_URL como una sola variable de entorno (incluye
# esquema mysql:// o postgresql://) en vez de host/usuario/password sueltos
# — ver backend/settings.py.
dj-database-url>=3.0,<4

# Archivos estÃ¡ticos del admin en Render
whitenoise[brotli]>=6.9,<7

# Generación de documentos
reportlab==5.0.0
pillow==12.2.0
openpyxl==3.1.5
python-pptx==1.0.2

# Web Push (notificaciones del sistema operativo vía Service Worker, con la
# app/pestaña cerrada) — firma los envíos con las llaves VAPID de
# backend/settings.py. Ver services/notificaciones.py.
pywebpush==2.4.0
